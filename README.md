# Dev Stack Builder

Dev Stack Builder is a responsive React application where developers can explore popular technologies and build a personalized development stack for their projects.


## Features

- Explore 15 technologies across frontend, backend, database, language, styling, DevOps, and tools categories.
- Add technologies to a personal stack while preventing duplicate selections.
- Remove individual technologies or clear the complete stack.
- Use the application across responsive mobile, tablet, and desktop layouts.
- Open navigation links through a functional mobile hamburger menu.
- Receive clear feedback through React Toastify notifications.
- View a loading state while technology data is loaded.
- Maintain technology information separately in a JSON file.

## Technologies Used

- React.js
- JavaScript ES6+
- Tailwind CSS
- Vite
- React Toastify
- JSON

## Project Structure

```text
ph-b14-assignment5/
├── assets/
│   ├── banner-stack.png
│   └── logo-text.png
├── src/
│   ├── components/
│   │   ├── Footer.jsx
│   │   ├── Hero.jsx
│   │   ├── Navbar.jsx
│   │   ├── StackSidebar.jsx
│   │   ├── TechnologyCard.jsx
│   │   └── TechnologyGrid.jsx
│   ├── data/
│   │   └── technologies.json
│   ├── App.jsx
│   ├── index.css
│   └── main.jsx
├── ui/
├── index.html
├── package.json
└── vite.config.js
```

The `components` folder contains reusable interface components, while `data` stores the technology information used to generate the cards.

## How to Run Locally

1. Clone the repository:

   ```bash
   git clone https://github.com/rownak2385/ph-b14-assignment5.git
   ```

2. Enter the repository folder:

   ```bash
   cd ph-b14-assignment5
   ```

3. Install the dependencies:

   ```bash
   npm install
   ```

4. Start the development server:

   ```bash
   npm run dev
   ```

## Main Functionality

Technology cards are generated by mapping over the data in `technologies.json`. React state keeps track of the selected technologies and updates the Your Stack sidebar whenever an item is added or removed. After selection, a card's button is disabled and displays **✓ Added to Stack**.

Users can remove one technology or clear the entire stack. Toast messages provide feedback for stack actions, and the layout adapts to desktop, tablet, and mobile screen sizes.

## React Questions

### 1. What is JSX, and why is it used in React?

JSX lets us write HTML-like markup inside JavaScript. It makes React interfaces easier to describe, organize, and understand.

### 2. What is the difference between props and state?

Props are values passed from a parent component to a child component. State belongs to a component and can change while the application is running.

### 3. What does the useState hook do, and where was it used in this project?

`useState` stores values that can change and causes the interface to update when they do. This project uses it for the selected technology stack, loading status, and mobile menu state.

### 4. What does the useEffect hook do, and why was it needed to load the JSON data?

`useEffect` runs code after a component renders. It was used to load the technology data when the application starts and then update the loading state.

### 5. Why does every item in a .map() list need a unique key prop?

A unique `key` helps React identify which list item changed, was added, or was removed. This allows React to update the list correctly and efficiently.

### 6. What is conditional rendering? Show one place it was used.

Conditional rendering displays different content based on the current data or state. In Your Stack, an empty message appears when nothing is selected; otherwise, the selected technology items are displayed.

### 7. How do you pass data from a parent component to a child component, and how does a child send something back to the parent?

A parent sends data and functions to a child through props. The child can call a function received through props to send an action back to the parent.

## Screens / Responsive Design

The application supports:

- Desktop
- Tablet
- Mobile

## Author

Md. Rownak Hasan

## Repository

[https://github.com/rownak2385/ph-b14-assignment5](https://github.com/rownak2385/ph-b14-assignment5)

## Live Demo

[View Dev Stack Builder](https://rh-dev-stack.vercel.app/)
